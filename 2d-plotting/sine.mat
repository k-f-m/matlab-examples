% This code generates a sine wave plot.

% Define the x values using a range from 0 to 2*pi
x = linspace(0,2*pi);

% Calculate the y values using the sine function
y = sin(x);

% Plot the sine wave using the x and y vectors
plot(x,y);